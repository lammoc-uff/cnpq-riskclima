# CMIP6 blocking workflow

The CMIP6 workflow generates a model-specific 1980–2010 historical climatology and calculates the daily blocking series for the configured experiment.

Run the stages independently:

```bash
make climatology-cmip6
make series-cmip6
```
