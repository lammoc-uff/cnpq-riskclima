# ERA5 raw data

The ERA5 preprocessing workflow downloads pressure-level NetCDF fields from the public ARCO ERA5 Google Cloud bucket into `{ERA5_RAW_DIRECTORY}/{YEAR}/nc/`. It also creates daily derived fields and persistent yearly NetCDF files in that directory.

These files are downloaded or generated by `riskclima-sacz-process-era5` and are not committed to Git.
