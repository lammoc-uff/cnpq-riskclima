# SACZ area boundary data

Place the complete `sams_index_calc_areas` shapefile bundle in this directory. The workflow expects the country or region polygons referenced by the SACZ model and uses the `area` attribute as the feature index.

The shapefile and its sidecar files are pending addition. Record the provider, dataset version, CRS, source URL, and access date here when the official asset is supplied.
